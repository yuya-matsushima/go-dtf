language: go
sudo: false
branched:
  only:
    - master
    - develop
    - feature/*
go:
  - 1.4.3
  - 1.5.4
  - 1.6.4
  - 1.7.5
  - 1.8
  - tip
install:
  - go get github.com/golang/lint/golint
script:
  - test -z "$(golint ./... | tee /dev/stderr)"
  - go vet .
  - go test -v .
  - go test -bench . -benchmem
matrix:
  allow_failures:
    - go: tip
